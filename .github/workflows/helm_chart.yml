name: Helm Chart OCI Publishing

on:
  push:
    tags: ["v*.*.*"]
    paths-ignore:
      - ".github/workflows/**"
      - "docs/**"

  workflow_dispatch:

env:
  HELM_REGISTRY: ghcr.io
  HELM_NAMESPACE: ${{ github.repository_owner }}

jobs:
  publish-helm-chart:
    runs-on: ubuntu-latest

    if: ${{ github.actor != 'dependabot[bot]' && github.actor != 'renovate[bot]' }}

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: "latest"

      - name: Setup ORAS
        uses: oras-project/setup-oras@v1

      - name: Log into Helm registry
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | helm registry login ${{ env.HELM_REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Log into OCI registry for ORAS
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | oras login ${{ env.HELM_REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Package and push Helm chart to OCI registry
        run: |
          cd charts/go-hello-world
          VERSION=${GITHUB_REF_NAME#v}
          # Update version and name in Chart.yaml temporarily for this build
          sed -i "s/version: .*/version: $VERSION/" Chart.yaml
          sed -i "s/appVersion: .*/appVersion: \"$VERSION\"/" Chart.yaml
          sed -i "s/name: .*/name: go-hello-world-chart/" Chart.yaml
          # Package the chart (will now create go-hello-world-chart-$VERSION.tgz)
          helm package . --destination /tmp
          # Push to OCI registry (Helm uses the chart name from Chart.yaml metadata)
          helm push /tmp/go-hello-world-chart-$VERSION.tgz oci://${{ env.HELM_REGISTRY }}/${{ env.HELM_NAMESPACE }}

      - name: Push Artifact Hub metadata to OCI registry
        run: |
          # Push artifacthub-repo.yml as a separate OCI artifact with the special tag
          oras push \
            ${{ env.HELM_REGISTRY }}/${{ env.HELM_NAMESPACE }}/go-hello-world-chart:artifacthub.io \
            --config /dev/null:application/vnd.cncf.artifacthub.config.v1+yaml \
            charts/go-hello-world/artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml
